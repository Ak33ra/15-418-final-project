name: mps-multitenant
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip>=23.0
  - pytorch-cuda=12.1 
  - pytorch
  - torchvision
  - pip:
      - transformers>=4.40
      - accelerate>=0.30
      - sentencepiece
      - datasets
      - matplotlib
      - seaborn
      - pandas
      - tqdm

