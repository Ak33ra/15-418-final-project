experiment_name: "pair_distilbert"
mps: True
sleep_duration: 120
device: "cuda"

tenants:
  - model_name: "distilbert-base-uncased"
    name: "distilbert_1"
    batch_size: 8
    seq_len: 128
    num_iters: 100
    num_warmup: 10

  - model_name: "distilbert-base-uncased"
    name: "distilbert_2"
    batch_size: 8
    seq_len: 128
    num_iters: 100
    num_warmup: 10
